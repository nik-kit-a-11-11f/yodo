// Техническое задание от стартапа "Connect":
// Улучшить пользовательский опыт в веб-чате
// Автоматически очищать поле ввода после отправки сообщения

// Текущая проблема:
// После отправки сообщения текст остается в поле ввода
// Пользователь вынужден удалять его вручную

// Задача:
// 1. Найти поле ввода сообщения и форму
// 2. Добавить обработчик отправки формы
// 3. После отправки сообщения очистить поле ввода
// 4. Вернуть фокус в поле ввода для быстрого набора следующего сообщения

// TODO: Ваша задача - улучшить UX чата

// Шаг 1: Найти необходимые элементы
// - Форму с id="message-form"
// - Поле ввода с id="message-input"
// - Контейнер сообщений с id="chat-messages"

// Шаг 2: Добавить обработчик события submit на форму

// Шаг 3: В обработчике отменить стандартное поведение формы

// Шаг 4: Получить текст из поля ввода

// Шаг 5: Проверить, что поле не пустое

// Шаг 6: Создать и добавить новое сообщение в чат

// Шаг 7: ОЧИСТИТЬ ПОЛЕ ВВОДА (основная задача урока!)

// Шаг 8: Вернуть фокус в поле ввода

// Шаг 9: Прокрутить чат к последнему сообщению

// Базовый код для добавления сообщений (уже работает):
function addMessage(text, isUser = true) {
    const chatMessages = document.getElementById('chat-messages');
    const messageDiv = document.createElement('div');
    messageDiv.className = `message ${isUser ? 'user' : 'system'}`;
    
    const now = new Date();
    const timeString = now.getHours().toString().padStart(2, '0') + ':' + 
                      now.getMinutes().toString().padStart(2, '0');
    
    messageDiv.innerHTML = `
        ${text}
        <div class="message-time">${timeString}</div>
    `;
    
    chatMessages.appendChild(messageDiv);
    
    // Прокрутка к последнему сообщению
    chatMessages.scrollTop = chatMessages.scrollHeight;
}

// Ваш код для улучшения UX ниже этой строки:
